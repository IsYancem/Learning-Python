# Ejercicio 2. Convertir radianes a grados.
import math

radianes = float(input('Inserte el numero de radianes:'))

grados = (180 * radianes)/math.pi

print('El numero de grados es: {}'.format(grados))
